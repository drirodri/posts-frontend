// Atom: Input component
// TODO: Implement input component with variants (text, email, password, etc.)
// Props: type, placeholder, value, onChange, error, label, etc.

export default function Input() {
  return null;
}
