// Atom: Text component
// TODO: Implement text component with variants (heading, body, caption, etc.)
// Props: variant, size, color, weight, children, as (h1, h2, p, span), etc.

export default function Text() {
  return null;
}
